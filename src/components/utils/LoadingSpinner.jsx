import { StyledLoadingSpinner } from "./LoadingSpinner.styled";

const LoadingSpinner = () => {
  return (
    <StyledLoadingSpinner className="loading-spinner"></StyledLoadingSpinner>
  );
};

export default LoadingSpinner;
